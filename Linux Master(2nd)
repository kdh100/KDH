1. 프린터 작업
    * 명령어
        - lpr : 프린터 출력
        - lrq : 프린터 큐 목록 확인
        - lpstat : 작업번호 확인
        - cancel : 확인된 작업번호 취소
2. 하드웨어
    * 프린터
        - LPRng, CUPS
    * 사운드 카드
        - OSS, ALSA
    * 스캐너
        - SANE, XSANE
3. 소스 파일 설치
    * make
        - PHP, SAMBA, Apache HTTP
    * cmake
        - MySQL
    * 설치 순서
        - configure > make > make install
            # configure 작업 후 생성되는 파일 명 : Makefile
4. 패키지
    * 온라인 기반
        - apt-get(debian), yum(redhat), zypper(suse)
    * 비온라인 기반
        - dpkg(debian), rpm(redhat), YaST(suse)