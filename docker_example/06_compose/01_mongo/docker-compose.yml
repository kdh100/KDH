# Use root/example as user/password credentials
version: '3.1'

services:
  # docker network create 01_mongo_default
  # docker run -d --name 01_mongo-mongo-1 --restart=always \
  # --network 01_mongo_default \
  # -e MONGO_INITDB_ROOT_USERNAME=root \
  # -e MONGO_INITDB_ROOT_PASSWORD=example \
  # mongo
  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
  
  # docker run -d --name 01_mongo-mongo-express-1 --restart=always \
  # --network 01_mongo_default -p 8081:8081 \
  # -e ME_CONFIG_MONGODB_ADMINUSERNAME=root \
  # -e ME_CONFIG_MONGODB_ADMINPASSWORD=example \
  # -e ME_CONFIG_MONGODB_URL="mongodb://root:example@01_mongo-mongo-1:27017" \
  # mongo-express
  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: example
      ME_CONFIG_MONGODB_URL: mongodb://root:example@mongo:27017/